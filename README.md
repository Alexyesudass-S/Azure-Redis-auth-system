# Azure-Redis-auth-system
A simple authentication system built using Azure Virtual Machine, Azure Cache for Redis, Node.js, and HTML. Supports user registration, login, deletion, and viewing all users with Redis as the backend store.
